// SwiftPM public-headers umbrella. SwiftPM requires a C target to have an
// `include` directory; the real declarations live in the shared header reused by
// every platform. Path is relative to THIS file:
//   include/ -> Sources/archive_ffi/ -> archive_ffi/ -> macos/ -> archive_ffi/ (plugin root)
#include "../../../../../src/archive_ffi.h"
